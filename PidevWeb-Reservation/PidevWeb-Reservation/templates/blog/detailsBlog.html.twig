{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
    <h1>Blog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Titre_blg</th>
                <td>{{ blog.titreBlg }}</td>
            </tr>
            <tr>
                <th>Email_blg</th>
                <td>{{ blog.emailBlg }}</td>
            </tr>
            <tr>
                <th>Contenu_blg</th>
                <td>{{ blog.contenuBlg }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blog_index') }}">back to list</a>

    <a href="{{ path('app_blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %}
